# Environment variables for biovector-eval
# Copy this to .env and fill in your values

# =============================================================================
# GenOMA Integration (Optional)
# =============================================================================
# GenOMA uses OpenAI for LLM-based ontology mapping (HPO)
# Required only if using the GenOMA adapter for questionnaire harmonization
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_key_here

# Optional: Override the default model for GenOMA
# Default is gpt-4, but gpt-3.5-turbo can be used for cost savings
# GENOMA_MODEL=gpt-4
